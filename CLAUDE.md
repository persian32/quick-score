# quick-score — 작업 시 알아둘 것

그룹별 단어시험 채점 웹앱. **동료 교사에게 넘겨줄 것**이고, 실제 사용자는 각 반의
영어 도우미(학생)다. 자세한 배경과 결정 과정은 `작업일지.md`, 구조는 `README.md`.

## 손대기 전에 읽을 것

- `README.md` — 구조·폴더·검사 목록
- `작업일지.md` — 왜 이렇게 만들었는지, 무엇을 고쳤는지 (날짜별)
- `배포안내.md` — 쓰실 분에게 주는 설치 안내. 코드를 바꾸면 여기도 맞춰야 함

## 고칠 때 반드시 지킬 것

**① 검사를 전부 돌린다**

```bash
node 검사/열계산검사.js      # 그룹 분배 · 열 위치
node 검사/그룹조정검사.js    # ＋/－ 조작 불변식
node 검사/날짜표시검사.js    # 날짜 표시
node 검사/화면요소검사.js    # 화면·코드·CSS 아귀 맞춤
node 검사/서버함수검사.js    # 서버 코드 정합성
```

문법 검사(`node --check`)만으로는 부족하다. 여기 있는 검사들은 전부
**실제로 낸 버그를 다시 못 내게** 만든 것이다. 새 검사를 추가할 때는
일부러 깨뜨려서 잡히는지 확인할 것.

**② 미리보기를 다시 만든다**

```bash
python3 도구/미리보기만들기.py    # Index.html 을 고친 뒤
python3 도구/교사화면만들기.py    # 시트 배치를 바꾼 뒤
```

`docs/preview.html` 과 `docs/teacher.html` 은 손으로 고치지 말 것. 생성물이다.

**③ 파일이 여러 개 바뀌면 전부 알려준다**

사용자는 Apps Script 편집기에 **손으로 붙여넣는다.** `Code.gs` 만 바뀐 줄 알고
`Tests.gs` 를 안 고쳐서 하루를 날린 적이 있다. 바뀐 파일을 이름과 줄 수로 명시할 것.

**④ 붙여넣기 → 저장 → 재배포 순서를 안내한다**

Apps Script 는 배포 시점의 코드를 사진 찍어둔다. 편집기에서 고치고 자체 점검을
통과해도 **웹앱은 옛 코드로 돈다.** `배포 관리 → 연필 → 새 버전 → 배포`
(`새 배포` 는 주소가 바뀌므로 안 됨).

## 설계상 건드리면 안 되는 것

| | 이유 |
|---|---|
| `MAX_STUDENTS = 40` 고정 폭 | 학생수를 바꿔도 열이 안 움직여야 점수가 남는다 |
| `MAX_GROUPS = 10` 고정 폭 | `전체` 시트가 반마다 같은 위치를 참조한다 |
| 잠금 열 = B (고정) | 교사가 매주 누르는 유일한 칸. 스크롤 없이 보여야 한다 |
| 평균은 시트 `AVERAGE` 수식 | 빈칸(결석)을 알아서 빼준다. 코드로 옮기면 버그만 는다 |
| `manifest` 에 `start_url` 없음 | 넣으면 홈 화면 아이콘이 `?u=` 를 잘라먹는다 |
| 도우미 화면에 순위 없음 | 4~5명 그룹은 결석 한 명으로 순위가 뒤집힌다 |

## 사용자에 대해

코딩 초보자(파이썬 초중급)다. 화면을 직접 보고 판단하는 것을 중시한다.

- 폰 UI를 고쳤으면 **확인할 방법까지** 만들어 드릴 것 (미리보기 + 로컬 서버 주소)
- 붙여넣을 코드는 `open -e` 로 텍스트편집기에 띄워 드릴 것.
  `.html` 은 TextEdit 이 렌더링해버리므로 `.txt` 로 복사해서 열 것
- 터미널 명령 블록과 "붙여넣을 코드" 를 구분해서 보여줄 것 (혼동한 적 있음)
- 명령은 실행 전에 무엇을 하는지 한 줄로 말할 것

## 지금 상태

배포까지 검증 완료. 다른 계정에서 `/copy` 로 사본을 만들어 코드가 따라가고
독립적으로 배포되는 것까지 확인했다. 남은 일은 `작업일지.md` 맨 아래 참고.
