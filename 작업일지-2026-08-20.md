# quick-score 개발 일지

**2026-08-20** · 그룹별 단어시험 채점 웹앱

- 저장소: https://github.com/persian32/quick-score
- 미리보기: https://persian32.github.io/quick-score/

---

## 배경

동료 교사의 요청. 영어 수업에서 단어시험을 치고 **그룹별로 채점**하는데, 영어 도우미(학생)가 종이 채점표에 점수를 적고 합계·평균을 손으로 계산한다.

그룹마다 인원이 달라서(결석·결번) 단순히 합계만 비교할 수 없고, **합계 ÷ 응시 인원**으로 평균을 내야 한다. 이 계산을 학생이 매주 손으로 하는 게 문제였다.

**목표:** 도우미가 사이트에 접속해 점수만 넣으면 더하기·나누기가 자동으로 되고 그룹 순위가 나오게.

---

## 만든 것

| 대상 | 기기 | 보는 것 |
|---|---|---|
| 영어 도우미 | 폰·태블릿 | 전용 입력 화면. 구글 계정 불필요 |
| 담당 교사 | 컴퓨터 | 구글 스프레드시트 그대로 |

### 도우미 화면
- 반 선택 + 비밀번호 → 회차 선택 → **한 화면에 한 그룹(4~5칸)** → 순위
- 그룹을 넘길 때마다 자동 저장 (저장 버튼 없음)
- 빈칸 = 결석. 인원수에서 자동 제외
- 폰 다크모드 자동 대응, 글자 크기 3단계(선택 기억)

### 교사 화면
- 스프레드시트를 열면 맨 위 3줄에 그룹별 누적 평균과 순위
- `전체` 시트에서 전 반 비교
- `잠금` 칸에 체크하면 그 회차는 학생이 못 고침
- `로그` 시트에 모든 수정 기록

---

## 설계에서 갈렸던 결정

| 고민 | 선택 | 이유 |
|---|---|---|
| 서버를 어디에 두나 | **Google Apps Script** | 서버 비용·관리 0. 스프레드시트가 곧 DB이자 교사 화면 |
| 평균 계산을 코드로? | **스프레드시트 `AVERAGE` 수식** | `AVERAGE`가 빈칸을 무시하는 성질이 "결석자 제외"와 정확히 일치. 조건문이 통째로 사라짐 |
| 칸에 그룹 합계? 개인 점수? | **개인 점수** | 이게 더하기를 없애는 지점. 입력된 칸 수가 곧 응시 인원이라 인원 셀 필요도 없어짐 |
| 로그인 방식 | **반별 비밀번호** | 도우미가 학교 구글 계정을 자주 잊음. 카톡 인앱 브라우저에서도 그냥 열림 |
| 종이 표를 그대로 옮기나 | **한 화면에 한 그룹** | 폰 세로에 6×20 격자는 칸이 손톱만 해짐. 좁은 쪽에 맞춰야 태블릿에서도 됨 |
| 누구 계정에 두나 | **동료 교사 계정** | 학생 점수는 담당 교사가 소유해야 함. "사본 만들기" 링크로 넘김 |

### 보안 — 완전 차단 대신 흔적 남기기

상이 걸린 활동이라 조작 동기는 실재한다. 구글 로그인을 못 쓰는 이상 비밀번호는 새는 방벽이므로, 두 겹을 더 뒀다.

1. **수정 로그** — 누가 언제 어느 칸을 뭐에서 뭐로. 고쳐도 들킨다
2. **회차 잠금** — 교사가 확정하면 서버가 쓰기를 거부한다

비밀번호가 샜다고 느끼면 설정 시트에서 한 칸만 고치면 즉시 교체된다. 재배포 불필요.

---

## 구조

```
[도우미 폰]  ──URL──▶  [Apps Script]  ──기록──▶  [구글 스프레드시트]
 Index.html            Code.gs                    선생님이 그냥 열어서 봄
                       비밀번호 확인
                       잠금 확인
                       로그 기록
```

**핵심 원칙:** 도우미의 폰은 창고(스프레드시트)에 직접 못 들어간다. 무조건 관리인(Apps Script)을 거친다. 그래서 학생이 소스 보기를 눌러도 비밀번호도 다른 반 점수도 안 보인다.

**일을 최대한 남에게 떠넘겼다.**

| 일 | 담당 | 내가 짠 코드 |
|---|---|---|
| 서버 운영·보안 패치 | 구글 | 0줄 |
| 합계·평균·순위 계산 | 스프레드시트 수식 | 0줄 |
| 반 전체 비교표 | 스프레드시트 수식 | 0줄 |
| 교사용 조회 화면 | 스프레드시트 자체 | 0줄 |
| 인증·저장·로그 | 직접 | 약 250줄 |
| 폰 화면 | 직접 | 약 300줄 |

---

## 오늘 잡은 버그

1. **새 시트는 기본 26열** — 학생이 27명만 넘어도 잠금 열(37번째)에 접근하다 죽었다. 종이 표가 24명 기준이라 넘어갈 뻔했다.
2. **순위 수식이 항상 B3만 보고 빈칸 판정** — 데이터 없는 그룹에서 `#N/A`
3. **잠긴 회차에서 뒤로가기** — 저장을 시도하다 막혀 화면이 안 넘어갔다
4. **그룹 수를 바꾸면 잠금 열이 밀림** — 그룹평균 자리를 10칸 고정으로 잡아 해결

---

## 검증

```
node 열계산검사.js      그룹 분배 · 열 위치        통과 19
node 그룹조정검사.js    ＋/－ 조작 불변식          통과 10
문법 검사               전 파일                    통과
```

`그룹조정검사`는 무작위로 5000번 눌러 **합계가 늘 학생수와 같고 0명 그룹이 안 생기는지**를 확인한다.

Apps Script 쪽 검사 16개는 설치 후 편집기에서 `runTests` 실행 — **아직 안 돌림.**

---

## 남은 일

1. 스프레드시트에 설치 → `runTests()` 로 `통과 16 / 실패 0` 확인
2. 테스트 데이터 지우고 스프레드시트 주소 끝을 `/copy` 로
3. 동료 교사에게: 미리보기 링크 + 사본 링크 + `배포안내.md`
4. 동료 교사가 비밀번호 정하고 배포 → 도우미에게 URL

---

## 배운 것

**도구가 이미 하는 일을 찾아내면 코드가 아니라 버그가 준다.** `AVERAGE`가 빈칸을 무시한다는 사실 하나로 결석 판정 로직이 통째로 사라졌다. 새 기능을 짜기 전에 쓰고 있는 도구가 그걸 이미 하는지 먼저 볼 것.

**변할 수 있는 것을 고정 폭 안에 가둬두면 그 바깥은 안 흔들린다.** 그룹평균 칸을 그룹 수만큼만 만들었더니 그룹을 6개→5개로 바꿀 때 잠금 열이 밀려 체크박스와 점수가 어긋났다. 10칸을 미리 비워두니 해결. 표 형태 데이터를 다룰 때 계속 쓸 요령.

**계산이 맞아도 실제 환경에서 터진다.** "새 시트는 26열"은 문서에도 잘 안 나오는 제약이라 코드만 봐선 안 보였다. 배포 후 `runTests()` 를 꼭 돌려야 하는 이유.

### 아직 혼자 못 쓰는 부분

`Index.html` 의 `drawGroup()` · `drawGroupEdit()` — **화면을 문자열로 조립해 다시 그리는 코드.** 계산 로직은 파이썬 지식이 거의 그대로 통하는데 이 부분만 결이 다르다. 공부할 때 여기부터.
