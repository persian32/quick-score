# quick-score — 설치 안내

그룹별 단어시험 채점 앱

컴퓨터로 15분이면 끝납니다. 한 번만 하면 됩니다.

---

## 1단계 · 스프레드시트 만들기

1. [sheets.new](https://sheets.new) 접속 → 빈 스프레드시트가 열립니다.
2. 왼쪽 위 제목을 **`quick-score`** 으로 바꿉니다.

> 이 파일이 앱의 전부입니다. 나중에 이 파일을 지우면 앱도 같이 사라집니다.

---

## 2단계 · 코드 붙여넣기

1. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭. 새 탭이 열립니다.
2. 왼쪽에 `Code.gs` 파일이 하나 있습니다. 안의 내용을 **전부 지우고**, 받은 `Code.gs` 내용을 붙여넣습니다.
3. 왼쪽 **파일** 옆의 **＋ → 스크립트** → 이름을 **`Tests`** 로 짓고, `Tests.gs` 내용을 붙여넣습니다.
4. 다시 **＋ → HTML** → 이름을 **`Index`** 로 짓고, `Index.html` 내용을 붙여넣습니다.

> ⚠️ 4번은 반드시 **HTML**을 골라야 합니다. 스크립트로 만들면 화면이 안 뜹니다.
> 파일 이름은 정확히 `Index` 여야 합니다 (대문자 I).

5. 💾 저장 버튼을 누릅니다.

---

## 3단계 · 시트 만들기

스프레드시트 탭으로 돌아가면 상단 메뉴에 **`quick-score`** 가 새로 생겨 있습니다.
(안 보이면 페이지를 새로고침하세요)

```
파일  수정  보기  삽입  서식  데이터  도구  확장 프로그램  도움말  [ quick-score ]
                                                                    └ 설치 / 갱신
                                                                    └ 자체 점검
```

**`quick-score → 설치 / 갱신`** 을 클릭합니다.
3. **처음 한 번만** 권한 승인 창이 뜹니다:
   - `권한 검토` → 내 구글 계정 선택
   - "Google에서 확인하지 않은 앱입니다" 경고 → **고급 → quick-score(안전하지 않음)으로 이동**
   - `허용`

> 이 경고는 정상입니다. 구글에 등록비를 내고 심사받은 앱이 아니라서 뜨는 것이고,
> 내가 내 계정에서 내 스프레드시트를 고치는 코드이므로 위험하지 않습니다.

4. 스프레드시트 탭으로 돌아오면 **설정 / 로그 / 전체** 시트가 생겨 있습니다.

> 실행 로그에 `설정 시트에 학생수를 채우고 다시 실행하세요` 라고 나오는 것이 정상입니다.
> 반별 시트는 학생수를 채운 뒤에 만들어집니다 — 다음 단계입니다.

---

## 4단계 · 우리 반 정보 넣기

**설정** 시트를 엽니다.

1반부터 11반까지 이미 만들어져 있고, **비밀번호와 학생수가 비어 있습니다**(분홍색). 이 두 칸을 채워야 그 반을 쓸 수 있습니다.

**설치 직후 (이 상태로 받습니다)**

| 반이름 | 비밀번호 | 학생수 | 그룹당인원 |
|---|---|---|---|
| 1반 | | | 4 |
| 2반 | | | 4 |
| … | | | 4 |

**채운 뒤**

| 반이름 | 비밀번호 | 학생수 | 그룹당인원 |
|---|---|---|---|
| 1반 | 3141 | 25 | 4 |
| 2반 | 2718 | 22 | 4 |
| … | … | … | … |

- **학생수** 는 그 반 마지막 번호입니다 (결석은 신경 쓰지 마세요. 앱이 알아서 뺍니다).
- 그룹 인원을 `4,4,4,5` 처럼 직접 적어두었다가 학생수를 바꾸면 합계가 안 맞게 됩니다. 그때는 `설치 / 갱신` 이 **자동 분배로 되돌리고 알려줍니다.**
- 학생수는 **나중에 바꿔도 됩니다.** 전학생이 오면 숫자를 고치고 `quick-score → 설치 / 갱신` 만 누르세요. 그룹 나누기가 새 인원에 맞춰지고 **쌓아둔 점수는 그대로 남습니다.** (최대 40명)
- **그룹당인원** 은 목표 인원입니다. 딱 안 떨어지면 남는 학생을 뒤쪽 그룹에 한 명씩 얹습니다.
  25명을 4명씩 → `4 4 4 4 4 5` (6그룹). **1명짜리 그룹은 생기지 않습니다.**
  5명씩으로 바꾸면 `5 5 5 5 5` 로 딱 떨어집니다.
- 비밀번호가 비어 있는 반은 아무도 로그인할 수 없습니다.
- 다 적었으면 **`quick-score → 설치 / 갱신`** 을 다시 클릭합니다. **이때 반별 시트가 만들어집니다.**
- 학생수를 안 채운 반은 건너뛰고, 실행 로그가 어느 반을 건너뛰었는지 알려줍니다. 나중에 채우고 다시 실행하면 됩니다.

> 이미 있는 반 시트는 다시 실행해도 안 지워집니다. 점수가 날아갈 걱정 없습니다.

---

## 5단계 · 배포 (링크 만들기)

Apps Script 탭에서:

1. 오른쪽 위 **배포 → 새 배포**
2. 톱니바퀴 ⚙️ → **웹 앱** 선택
3. 이렇게 맞춥니다:

   | 항목 | 값 |
   |---|---|
   | 설명 | 아무거나 (예: `v1`) |
   | 다음 사용자로 실행 | **나** |
   | 액세스 권한이 있는 사용자 | **모든 사용자** |

   > "모든 사용자"로 해야 도우미가 **구글 로그인 없이** 들어올 수 있습니다.
   > 대신 앱이 반별 비밀번호로 직접 막습니다.

4. **배포** → 나오는 **웹 앱 URL** 을 복사합니다.

이 주소를 도우미에게 카톡으로 보내면 끝입니다.

---

## 6단계 · 도우미에게 안내할 것

**5단계에서 복사한 주소를 그대로 보내지 마세요.** 대신 아래처럼 감싸서 보냅니다.

> 💡 외우실 필요 없습니다. **https://persian32.github.io/quick-score/** 에 가면
> 배포 주소를 붙여넣는 칸이 있고, 학생용 링크를 만들어 줍니다.

```
https://persian32.github.io/quick-score/app.html?u=여기에_복사한_주소
```

예시:

```
https://persian32.github.io/quick-score/app.html?u=https://script.google.com/macros/s/AKfycb.../exec
```

이렇게 하면 홈 화면에 추가했을 때 **quick-score 아이콘과 이름**이 제대로 붙습니다.
그냥 script.google.com 주소로 추가하면 구글이 정한 아이콘이 뜹니다 (아래 설명 참고).

카톡에 링크와 함께 이렇게 보내주세요.

> quick-score 채점 링크야. 우리 반 비밀번호는 **3141**.
> 처음 열면 **"홈 화면에 추가"** 해두면 앱처럼 바로 열려.
> ㆍ아이폰: 아래 공유 버튼 → 홈 화면에 추가
> ㆍ안드로이드: 오른쪽 위 ⋮ → 홈 화면에 추가

### 왜 주소를 감싸나요

Apps Script 페이지는 구글이 만든 틀 안에서 돌아갑니다. 그래서 앱 아이콘을 지정하는
태그를 우리가 넣을 자리가 없고, 홈 화면에 추가하면 구글 아이콘이나 화면 캡처가 붙습니다.

`app.html` 은 아이콘과 이름만 달아주고 그 안에서 실제 앱을 띄우는 얇은 껍데기입니다.
도우미가 보는 화면과 동작은 완전히 같습니다.

도우미가 하는 일:
1. 반 고르고 비밀번호 입력
2. **＋ 새 시험 시작** (또는 지난 시험 날짜를 눌러 고치기)
3. 그룹별로 점수 입력 → **다음** (자동 저장됨)
4. 다 넣으면 "다 넣었습니다" 화면 — **그날 그룹별 평균**이 나옵니다

**그날 평균은 보이고, 순위는 안 보입니다.** 학생이 "오늘 우리 그룹 몇 점이야?"
하고 물으면 도우미가 답할 수 있게 그룹별 평균을 그룹 번호 순서로 보여줍니다.
등수를 매기거나 높은 순으로 줄 세우지는 않습니다 — 4~5명 그룹은 결석 한 명으로
등수가 뒤집혀서, 매주 등수를 보여주면 실력이 아니라 그날 출결에 학생들이
반응하게 됩니다. 최종 순위는 선생님이 시트에서 보고 발표 시점을 정합니다.

**지난 시험 평균**은 시험 목록에서 그 날짜 옆 **평균** 버튼을 누르면 됩니다.
점수 칸은 열리지 않으니 실수로 고칠 일이 없고, 확정된(🔒) 시험도 볼 수 있습니다.

**결석한 학생은 칸을 비워두면 됩니다.** 0점으로 넣지 마세요 — 평균이 깎입니다.

---

## 평소 사용법

### 결과 보기
스프레드시트에서 반 시트를 엽니다. **맨 위 3줄**에 그룹별 누적 평균과 순위가 있습니다.
여러 반을 한눈에 비교하려면 **전체** 시트를 보세요.

### 시험 확정하기 (수정 잠그기)
반 시트에서 그 날짜 행의 **B열 잠금** 칸에 체크 ✅.
그 순간부터 도우미가 그 시험을 못 고칩니다. A열과 B열은 고정이라 항상 왼쪽에 보입니다.

### 시험 날짜
**A열이 시험 날짜입니다.** 도우미가 그 시험에 처음 점수를 저장할 때 자동으로 적힙니다.
도우미가 다음날 입력했다면 날짜가 하루 밀리는데, 시트에서 직접 고치면 됩니다.
**한 번 고쳐두면 다시 덮어쓰지 않습니다.** 앱은 그 칸이 비어 있을 때만 날짜를 적으므로,
고친 뒤 도우미가 점수를 더 넣거나 고쳐도 날짜는 그대로 남습니다.

### 최종 순위는 언제 보나
반 시트 **3~4행**이 그 시점까지의 누적 평균과 순위입니다. 따로 나타나는 시점이 없고
시험을 입력할 때마다 갱신됩니다. **마지막 시험까지 입력을 마치고 열어보면 그것이 최종**이고,
그 행을 잠그면 더 이상 안 바뀝니다.

반마다 시험 횟수가 달라도 됩니다. 순위는 반 안에서만 매기고 반끼리 비교하지 않습니다.
`전체` 시트 맨 오른쪽에 반별 회차 수가 함께 나옵니다.

### 비밀번호 바꾸기
**설정** 시트의 비밀번호 칸만 고치면 즉시 적용됩니다. 재배포 필요 없습니다.

### 누가 점수를 고쳤는지 확인
**로그** 시트를 보세요. 시각·반·회차·학생·이전값·새값이 전부 남습니다.

---

## 문제가 생기면

| 증상 | 해결 |
|---|---|
| 링크를 열면 빈 화면 / 오류 | HTML 파일 이름이 정확히 `Index` 인지 확인 |
| 홈 화면 아이콘으로 열면 "주소를 붙여넣으세요" 가 뜸 | 선생님께 받은 **원래 링크**(`app.html?u=…`)를 브라우저에서 다시 열고, 거기서 홈 화면에 추가하세요 |
| 코드를 고쳤는데 반영이 안 됨 | **배포 → 배포 관리 → ✏️ → 버전: 새 버전 → 배포**. 새 배포를 또 만들면 주소가 바뀌니 주의 |
| "비밀번호가 맞지 않습니다" | 설정 시트의 비밀번호에 앞뒤 공백이 없는지 확인 |
| 반을 추가했는데 안 보임 | 설정 시트에 줄을 넣고 `setup` 을 다시 실행했는지 확인 |
| 특정 반만 로그인이 안 됨 | `quick-score → 설치 / 갱신` 을 눌러보세요. 대부분 그 자리에서 고쳐집니다 |
| 로그인 버튼을 눌러도 반응 없음 | 그 반 비밀번호 칸이 비어 있는지 확인 |
| "학생수가 비어 있습니다" | 설정 시트의 학생수 칸을 채우고 다시 로그인 |
| 시험 줄이 모자람 | **＋ 새 시험 시작** 을 누르면 알아서 늘어납니다. 신경 쓸 것 없습니다 |
| 날짜가 하루 밀림 | 도우미가 다음날 입력한 경우입니다. 시트 A열에서 고치세요 |

### 잘 도는지 점검하고 싶을 때
스프레드시트 메뉴에서 **`quick-score → 자체 점검`** 을 클릭하세요.
`통과 21, 실패 0` 이 나오면 정상입니다. 테스트용 임시 반은 자동으로 지워집니다.

---

## 비용

**0원입니다.** 구글 Apps Script는 무료이고 결제 수단을 등록하는 곳 자체가 없습니다.
하루 총 실행시간 한도(일반 계정 약 90분)가 있지만, 이 앱은 저장 한 번에 1초도 안 걸려
반 여러 개를 매주 돌려도 한도의 1%를 넘지 않습니다.
